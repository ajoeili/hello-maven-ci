services:
  app:
    image: ghcr.io/ajsy0001/hello-maven:latest
    ports:
      - "8080:8080"

  database:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DB}
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
